server:
  port: 8003

spring:
  application:
    name: order_service
  datasource:
    url: jdbc:postgresql://localhost:5432/kade_orders
    username: kade
    password: kade
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
